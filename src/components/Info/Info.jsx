const Info = ({ handleOpenDrawer }) => {
  return (
    <div>
      <div className="buttonGreen" onClick={handleOpenDrawer}>
        Вернуться назад
      </div>
    </div>
  );
};

export default Info;
